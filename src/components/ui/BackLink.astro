---
import { useTranslations, tplStr } from "../../i18n";

interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;

const locale = Astro.currentLocale ?? "en";
const t = useTranslations(locale);
---

<a href={href} class="back-link">
  {tplStr(t.buttons.backLink, { section: label })}
</a>
